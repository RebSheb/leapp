# GitHub secret scanning does not support inline pragmas.
# Exclude intentional MSAL cache fixtures used only in unit tests.
paths-ignore:
  - "packages/core/src/services/azure-persistence-service.spec.ts"
